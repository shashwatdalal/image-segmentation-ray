ray_init:
  address: ray://gpu.instance:10001 # server address
  runtime_env:
    conda: ray-image-segmentation # conda env
    working_dir: /Users/shashwat_dalal/Documents/image-segmentation-ray/ # run in context of this directory when running code remotely
    excludes:
      - data # do not copy data directory